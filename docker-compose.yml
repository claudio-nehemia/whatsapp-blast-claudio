version: '3.8'

services:
  mongodb:
    image: mongo:6.0
    container_name: whatsapp_mongodb
    restart: always
    volumes:
      - mongo_data:/data/db
    networks:
      - whatsapp_net

  whatsapp-service:
    build:
      context: ./whatsapp-service
      dockerfile: Dockerfile
    container_name: whatsapp_node_gateway
    restart: always
    environment:
      - PORT=5000
      - BACKEND_CALLBACK_URL=http://backend:8000/api/whatsapp/status
    volumes:
      # Isolate session storage and mount the shared uploads volume at the same absolute path
      - whatsapp_sessions:/app/storage/session
      - backend_uploads:/app/storage/uploads
    depends_on:
      - backend
    networks:
      - whatsapp_net

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: whatsapp_rust_backend
    restart: always
    environment:
      - MONGO_URI=mongodb://mongodb:27017/whatsapp_blast
      - WHATSAPP_SERVICE_URL=http://whatsapp-service:5000
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - backend_uploads:/app/storage/uploads
    depends_on:
      - mongodb
    networks:
      - whatsapp_net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: whatsapp_react_frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - whatsapp_net

volumes:
  mongo_data:
  whatsapp_sessions:
  backend_uploads:

networks:
  whatsapp_net:
    driver: bridge
